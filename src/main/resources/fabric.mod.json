{
	"schemaVersion": 1,
	"id": "${id}",
	"version": "${version}",
	"name": "${name}",
	"description": "${description}",
	"authors": [
		"${author}"
	],
	"contact": {
		"homepage": "https://github.com/Ayydxn/FabricModTemplate",
		"sources": "https://github.com/Ayydxn/FabricModTemplate/issues"
	},
	"license": "${license}",
	"icon": "assets/fabric-mod-template/${icon_file}",
	"environment": "*",
	"entrypoints": {
		"main": [
			"com.ayydxn.fabricmodtemplate.FabricModTemplate"
		]
	},
	"mixins": [
		"fabric-mod-template.mixins.json"
	],
	"depends": {
		"minecraft": "~${minecraft_version}",
		"fabricloader": ">=${fabric_loader_version}",
		"fabric-api": "${fabric_api_version}"
	}
}