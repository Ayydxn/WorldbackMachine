{
	"schemaVersion": 1,
	"id": "${id}",
	"version": "${version}",
	"name": "${name}",
	"description": "${description}",
	"authors": [
		"${author}"
	],
	"contact": {
		"homepage": "https://github.com/Ayydxn/WorldbackMachine",
		"sources": "https://github.com/Ayydxn/WorldbackMachine",
		"issues": "https://github.com/Ayydxn/WorldbackMachine/issues"
	},
	"license": "${license}",
	"icon": "assets/${id}/${icon_file}",
	"environment": "*",
	"entrypoints": {
		"main": [
			"com.ayydxn.worldbackmachine.WorldbackMachineMod"
		],
		"modmenu": [
			"com.ayydxn.worldbackmachine.WorldbackMachineModMenuEntrypoint"
		]
	},
	"mixins": [
		"${id}.mixins.json"
	],
	"depends": {
		"minecraft": "~${minecraft_version}",
		"fabricloader": ">=${fabric_loader_version}",
		"fabric-api": "${fabric_api_version}",
		"yet_another_config_lib_v3": "${yacl_version}"
	}
}